<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
     <a class="navbar-brand" href="{{ path('app_home') }}">TaskFlow</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">🏠 Accueil</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home_presentation') }}">ℹ️ Présentation</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('task_index') }}">📋 Mes Tâches</a>
                    </li>
                    <a href="{{ path('app_logout') }}" class="btn btn-danger ms-3">
                        🔓 Déconnexion
                    </a>
                    
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-light ms-3">
                        🔐 Connexion
                    </a>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
